<%- include ('../../partials/head') %>
<%- include ('../../partials/navbar') %>
<div class="container">
    <br>
    <h1>Categorias</h1>
    <br>
    <a class="btn btn-success" href="/admin/categories/new">Criar nova categoria</a>
    <hr>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titulo</th>
                <th>Slug</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            <% categories.forEach(category => { %>
                <tr>
                    <th><%= category.id %></th>
                    <th><%= category.title %></th>
                    <th><%= category.slug %></th>
                    <th>
                        <a class="btn btn-warning" href="/admin/categories/edit/<%= category.id %>">Editar</a>
                        <form action="/categories/delete" method="post" onsubmit="deletionConfirmation(event, this)" style="display: inline;">
                            <input type="hidden" name="id" value="<%= category.id %>">
                            <button class="btn btn-danger">Deletar</button>
                        </form>
                        
                    </th>
                </tr>
            <% }) %>
            
        </tbody>
    </table>
</div>
<%- include ('../../partials/end') %>
<script>
    function deletionConfirmation (event, form){
        event.preventDefault()
        let decision = confirm("Você realmente deseja deletar essa categoria?") //retorna true or false
        if(decision){
            form.submit();
        }

    }
</script>